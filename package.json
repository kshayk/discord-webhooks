{
  "name": "discord_webhooks",
  "version": "1.0.0",
  "description": "webhook handler for multiple discord servers",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "prompt-confirm": "^1.2.0",
    "request": "^2.85.0",
    "yargs": "^11.0.0"
  }
}
